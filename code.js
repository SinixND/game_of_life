/*
//Initializing
var i = 0;
var itemClasses = [];
var icons = [];

//ids
itemClasses = ['main-weapon', 'sidearm', 'mask', 'backpack', 'chest', 'gloves', 'holster', 'kneepads'];

//function
function setBackgroundIcons() {
    for (i < itemClasses.length -1) {
        var element document.getElementByClassName(i);
        element.style.background = var(--cbg75), "url("icons/${i}.png")";
    }
}

//apply function
window.onload = setBackgroundIcons();
*/
